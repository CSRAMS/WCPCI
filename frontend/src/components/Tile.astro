---
import type { HTMLTag, Polymorphic } from "astro/types";

export type Props<T extends HTMLTag> = Polymorphic<{ as: T }>;

const { as: Component = "div", class: className, ...rest } = Astro.props;
---

<Component class:list={["rounded-lg bg-background-100 p-4", className ?? ""]} {...rest}>
    <slot />
</Component>
